services:
  sentiment_analysis:
    image: ghcr.io/taranis-ai/taranis-sentiment-analysis:latest
    environment:
      DEBUG: "${DEBUG:-False}"
<<<<<<< before updating
      MODEL: "${MODEL:-roberta}"          
      API_KEY: "${API_KEY:-testkey}"            
=======
      MODEL: "${MODEL:-<default_model>}"
      API_KEY: "${API_KEY:-testkey}"
      # add additional env vars
>>>>>>> after updating
    ports:
      - "${BOT_PORT:-5500}:8000"
    restart: unless-stopped